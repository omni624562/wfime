/*
 * Copyright 2024 The LimeIME Open Source Project
 */

package nan.toload.main.hd.candidate;

import android.content.Context;
import android.util.AttributeSet;

// Stub implementation to replace legacy CandidateExpandedView
public class CandidateExpandedView extends CandidateView {

    public CandidateExpandedView(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public CandidateExpandedView(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }

    // Override methods if necessary to prevent crashes/logic errors
}
